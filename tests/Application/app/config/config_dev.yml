imports:
    - { resource: "config.yml" }

doctrine:
    dbal:
        path: "%kernel.project_dir%/var/db_dev.sql"

framework:
    router:
        resource: "%kernel.project_dir%/app/config/routing_dev.yml"
    profiler:
        enabled: true
